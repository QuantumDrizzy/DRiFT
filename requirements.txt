# DRIFT — runtime dependencies (Python 3.10+)
# Mirrored in pyproject.toml. Prefer: pip install -e ".[dev]"  (pytest is the dev extra)
numpy>=1.24
scipy>=1.11          # sparse Hamiltonians + Lanczos ground state (Phase 3)
matplotlib>=3.7      # figures
networkx>=3.0        # optional: nicer graph layouts in the MaxCut figure (Phase 2)
